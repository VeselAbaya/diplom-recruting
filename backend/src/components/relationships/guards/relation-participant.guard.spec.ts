import { RelationParticipantGuard } from './relation-participant.guard';

describe('RelationParticipantGuard', () => {
  it('should be defined', () => {
    expect(new RelationParticipantGuard()).toBeDefined();
  });
});
