import { RequestDeclinerGuard } from './request-decliner.guard';

describe('RequestDeclinerGuard', () => {
  it('should be defined', () => {
    expect(new RequestDeclinerGuard()).toBeDefined();
  });
});
