import { IsMultipartInterceptor } from './is-multipart.interceptor';

describe('IsMultipartInterceptor', () => {
  it('should be defined', () => {
    expect(new IsMultipartInterceptor()).toBeDefined();
  });
});
