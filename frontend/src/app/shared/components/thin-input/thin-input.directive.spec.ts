import { ThinInputDirective } from './thin-input.directive';

describe('ThinInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ThinInputDirective();
    expect(directive).toBeTruthy();
  });
});
