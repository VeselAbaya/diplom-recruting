import { ExperienceSliderDirective } from './experience-slider.directive';

describe('ExperienceSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ExperienceSliderDirective();
    expect(directive).toBeTruthy();
  });
});
