import { OnlyInBrowserDirective } from './only-in-browser.directive';

describe('OnlyInBrowserDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyInBrowserDirective();
    expect(directive).toBeTruthy();
  });
});
