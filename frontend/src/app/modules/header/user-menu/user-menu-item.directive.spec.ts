import { UserMenuItemDirective } from './user-menu-item.directive';

describe('UserMenuItemDirective', () => {
  it('should create an instance', () => {
    const directive = new UserMenuItemDirective();
    expect(directive).toBeTruthy();
  });
});
