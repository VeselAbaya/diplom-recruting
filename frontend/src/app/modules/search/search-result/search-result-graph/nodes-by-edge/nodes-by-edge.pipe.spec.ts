import { NodesByEdgePipe } from './nodes-by-edge.pipe';

describe('NodesByEdgePipe', () => {
  it('create an instance', () => {
    const pipe = new NodesByEdgePipe();
    expect(pipe).toBeTruthy();
  });
});
